# **SQLD Day 31 – 종합 복습 / SQLD 전체 개념 총정리 (2025-11-12)**

> 📘 SQLD 1·2과목 통합 복습  
> 🧑‍💻 학습자: Jaerok Kim (Raprok612)

---

## **🎯 학습 목표**

- SQLD 전 범위 핵심 개념 및 실전 포인트 최종 정리  
- 1과목(데이터 모델링) + 2과목(SQL 활용) 연결 구조 이해  
- 자주 틀리는 함정 유형 및 기출 패턴 집중 복습  
- 시험 전날 마무리 점검용 요약 노트 완성

---

## **🧩 복습 구성 요약**

| 구분 | 주요 내용 | 핵심 포인트 |
|------|------------|--------------|
| **1과목** | 데이터모델링 / 정규화 / 관계 / 식별자 | ERD 작성 규칙, 정규화 단계별 의미 |
| **2과목** | SQL 기초 / JOIN / GROUP BY / 서브쿼리 / 트랜잭션 | SQL 실행 순서, 함수 구분, 집계 로직 |
| **공통** | 실전형 응용 / 함정 유형 | OUTER JOIN + WHERE / NULL 비교 / HAVING 위치 |

> 💡 단순 암기가 아닌 **“SQL 해석 순서” 중심의 사고 복습**이 중요하다.

---

## **🧠 핵심 개념 정리**

### **1️⃣ 데이터 모델링 핵심**
- 엔터티, 속성, 관계 → 식별자 관계로 확정  
- 정규화: 1NF → 2NF → 3NF → BCNF (함수적 종속 기반)  
- 반정규화: 성능을 위한 병합, 중복 허용 (조회 성능 개선)  

### **2️⃣ SQL 기본 구조**
```sql
SELECT 컬럼명
FROM 테이블명
WHERE 조건
GROUP BY 컬럼
HAVING 조건
ORDER BY 정렬기준;
```
- WHERE → GROUP BY → HAVING → SELECT → ORDER BY 순서로 실행됨

### **3️⃣ JOIN & 서브쿼리**
- INNER / LEFT / RIGHT / FULL JOIN의 결과 비교  
- WHERE절 조건 위치에 따라 결과 달라짐 (OUTER JOIN 주의)  
- 단일행 서브쿼리 vs 다중행 서브쿼리 구분 (IN, EXISTS, ANY, ALL)

### **4️⃣ 함수 및 집계**
- NVL, COALESCE, NULLIF 구분  
- RANK / DENSE_RANK / ROW_NUMBER 차이 명확히 구분  
- ROLLUP, CUBE, GROUPING 함수의 집계 구조 복습

### **5️⃣ 트랜잭션 & 무결성**
- COMMIT / ROLLBACK / SAVEPOINT 개념  
- 제약조건: PRIMARY, FOREIGN, UNIQUE, CHECK, NOT NULL

---

## **🧮 종합 실전 점검 예시**

**예제 1️⃣ – JOIN + HAVING 조합**
```sql
SELECT D.DEPTNO, D.DNAME, AVG(E.SAL) AS AVG_SAL
FROM DEPT D LEFT JOIN EMP E ON D.DEPTNO = E.DEPTNO
GROUP BY D.DEPTNO, D.DNAME
HAVING AVG(E.SAL) > 2000;
```

**예제 2️⃣ – 서브쿼리와 윈도우 함수**
```sql
SELECT ENAME, SAL,
       RANK() OVER (ORDER BY SAL DESC) AS RNK
FROM EMP
WHERE DEPTNO IN (SELECT DEPTNO FROM DEPT WHERE LOC = 'SEOUL');
```

**예제 3️⃣ – NULL 처리**
```sql
SELECT ENAME, NVL(COMM,0) AS COMM, COALESCE(BONUS,0) AS BONUS FROM EMP;
```

---

## **📚 최종 복습 체크리스트**
- [x] 데이터 모델링 개념 및 ERD 구조 복습  
- [x] 정규화 / 반정규화 개념 재정리  
- [x] JOIN / GROUP BY / 서브쿼리 / 윈도우 함수 오답 복습  
- [x] NULL, NVL, COALESCE, GROUPING 함수 차이 확인  
- [x] 트랜잭션 / 제약조건 흐름 이해  
- [x] 실제 기출문제 3회분 재풀이

---

## **💬 느낀점 / 메모**
- SQLD 전 과정을 다시 보니 **데이터 모델링과 SQL 구조의 연결성**이 핵심임을 체감했다.  
- 단순 쿼리 암기가 아닌, **“SQL이 데이터를 어떻게 읽고 변환하는가”**를 이해해야 함.  
- 시험 전날에는 복잡한 문제보다 **실수 예방 포인트 중심 복습**으로 마무리한다.

---

## **🔗 참고자료**
- SQLD 공식 교재 1·2권 (2025 최신판)  
- 아이리포 SQLD 강의 – 종합 복습편 (유튜브 79~80강)  
- Hyuncloud 기출문제 PDF – SQLD 전 회차 모음

---

🧠 **Growth-Log용 학습주제**  
> Day 31 – 종합 복습 / SQLD 전체 개념 총정리  

💾 **오늘의 커밋 명령어**
```bash
git add .
git commit -m "chore(log): add SQLD Day 31 – 종합 복습 / SQLD 전체 개념 총정리 (2025-11-12)"
git push origin main
```
