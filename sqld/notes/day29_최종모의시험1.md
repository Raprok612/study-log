# **SQLD Day 29 – 최종 모의시험 ① / 종합 실전 문제 풀이 (2025-11-10)**

> 📘 SQLD 2과목 | SQL 기본 및 응용  
> 🧑‍💻 학습자: Jaerok Kim (Raprok612)

---

## **🎯 학습 목표**

- SQLD 실전 감각 강화를 위한 복원형 모의시험 1회 수행  
- 제한 시간 내 문제 풀이 및 검증 로직 점검  
- 서브쿼리, GROUP BY, JOIN, 윈도우 함수 등 핵심문제 실전 적용  
- 실제 시험 환경과 동일한 흐름으로 풀이 후 오답 분석

---

## **🧠 이론 & 전략 정리**

### **1️⃣ 실전형 쿼리 풀이 전략**

| 영역 | 핵심 포인트 | 주의사항 |
|------|--------------|-----------|
| **SELECT 절** | 함수, CASE WHEN 구조 | ALIAS 재사용 불가 |
| **FROM / JOIN 절** | INNER / OUTER JOIN 구분 | WHERE 조건 위치 주의 |
| **GROUP BY / HAVING** | 다중 그룹 + 조건 | HAVING은 집계 후 필터링 |
| **서브쿼리** | 단일 vs 다중행 비교 | IN / EXISTS 구분 |
| **윈도우 함수** | PARTITION BY, ORDER BY | RANK / DENSE_RANK 차이 |
| **집합 연산자** | UNION / UNION ALL | 중복 여부 확인 |

> ⚡ 실전 시험에서는 함수형 문제보다 **결과 비교형 문제**가 빈출.

---

## **🧮 실전 예제 및 풀이**

**예제 1️⃣ – 다중행 서브쿼리와 ANY/ALL 구문**
```sql
SELECT ENAME, SAL
FROM EMP
WHERE SAL > ALL (SELECT SAL FROM EMP WHERE DEPTNO = 30);
```
- DEPTNO=30 부서의 최대급여보다 큰 급여만 출력  → ALL 사용

---

**예제 2️⃣ – JOIN + 집계 복합형**
```sql
SELECT D.DEPTNO, D.DNAME, COUNT(E.EMPNO) AS CNT, AVG(E.SAL) AS AVG_SAL
FROM DEPT D LEFT JOIN EMP E ON D.DEPTNO = E.DEPTNO
GROUP BY D.DEPTNO, D.DNAME
HAVING AVG(E.SAL) > 2000;
```
- LEFT JOIN에서 집계조건을 HAVING으로 필터링해야 함 (WHERE X)

---

**예제 3️⃣ – 윈도우 함수 실전형**
```sql
SELECT DEPTNO, ENAME, SAL,
       RANK() OVER (PARTITION BY DEPTNO ORDER BY SAL DESC) AS R1,
       DENSE_RANK() OVER (PARTITION BY DEPTNO ORDER BY SAL DESC) AS R2
FROM EMP;
```
- 동일 부서 내 급여순위 비교, 동순위 처리 차이 파악

---

**예제 4️⃣ – GROUPING SETS 실습**
```sql
SELECT DEPTNO, JOB, SUM(SAL)
FROM EMP
GROUP BY GROUPING SETS ((DEPTNO), (JOB));
```
- ROLLUP/CUBE보다 명시적 조합 지정 가능 (시험 출제 빈도 낮음)

---

## **🧾 모의시험 결과 요약**

| 구분 | 문항 | 주요 주제 | 결과 | 비고 |
|------|------|------------|-------|------|
| Part 1 | 01–05 | 서브쿼리 / 비교 연산 | ✅ | ALL, EXISTS 구분 정확 |
| Part 2 | 06–10 | JOIN + 집계 | ⚠ | HAVING 조건 순서 주의 |
| Part 3 | 11–15 | 윈도우 함수 | ✅ | 순위 함수 결과 확인 |
| Part 4 | 16–20 | 집합 연산 / GROUPING | ✅ | UNION vs UNION ALL 구분 |

📘 출처: 아이리포 SQLD 강의 – 복원 모의 1회 (유튜브 73~75강)

---

## **💬 느낀점 / 메모**

- 실전 시험처럼 시간을 제한하고 풀면 체감 난도가 높아진다.  
- 특히 HAVING, GROUPING, OUTER JOIN 조건 위치는 함정 포인트.  
- 문제마다 “SQL 실행 순서”를 명확히 떠올리며 풀어야 실수 방지 가능.  
- 내일 2회차 모의시험에서는 오답 유형별 시간 단축 전략을 병행할 것.

---

## **🔗 참고자료**
- 아이리포 SQLD 강의 – 복원 모의 1회 (유튜브 73~75강)
- SQLD 공식 교재 2권 p.363~370
- SQLD 실전 모의문제 PDF (Hyuncloud)

---

🧠 **Growth-Log용 학습주제**  
> Day 29 – 최종 모의시험 ① / 종합 실전 문제 풀이  

💾 **오늘의 커밋 명령어**
```bash
git add .
git commit -m "chore(log): add SQLD Day 29 – 최종 모의시험 ① / 종합 실전 문제 풀이 (2025-11-10)"
git push origin main
```
