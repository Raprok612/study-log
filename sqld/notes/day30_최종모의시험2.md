# **SQLD Day 30 – 최종 모의시험 ② / 실전 응용형 문제 풀이 (2025-11-11)**

> 📘 SQLD 2과목 | SQL 기본 및 응용  
> 🧑‍💻 학습자: Jaerok Kim (Raprok612)

---

## **🎯 학습 목표**

- SQLD 실전 대비 복원형 모의 2회 문제 풀이 및 응용 패턴 분석  
- 시간 내 풀이 능력 향상 및 정답 검증 로직 강화  
- JOIN, 서브쿼리, 윈도우 함수, 트랜잭션 등 실전 함정 유형 완전 정복  
- 시험 직전, 실전형 SQL 문제 접근 전략 점검

---

## **🧠 이론 & 전략 정리**

### **1️⃣ 실전 응용 포인트**

| 구분 | 핵심 개념 | 주의사항 |
|------|------------|-----------|
| **JOIN** | OUTER JOIN의 WHERE 조건 위치 | INNER로 변질 주의 |
| **서브쿼리** | IN vs EXISTS 성능 및 결과 비교 | NULL 포함 시 결과 상이 |
| **윈도우 함수** | PARTITION + ORDER BY | 순위 중복 처리 확인 |
| **집합 연산자** | UNION ALL, INTERSECT | 컬럼 수·데이터타입 일치 필수 |
| **트랜잭션 제어** | COMMIT / ROLLBACK | 자동 커밋 여부 구분 |
| **무결성 제약조건** | FOREIGN KEY / CHECK | 삭제·삽입 시 제약 확인 |

> ⚙️ 2회차 모의는 1회보다 **응용·복합 쿼리 비중이 높음.**

---

## **🧮 예제 및 실습**

**예제 1️⃣ – EXISTS vs IN 비교**
```sql
SELECT ENAME
FROM EMP E
WHERE EXISTS (SELECT 1 FROM DEPT D WHERE D.DEPTNO = E.DEPTNO);
```
- EXISTS: 조건 만족하는 레코드 존재 여부만 판단  
- IN은 NULL 값 포함 시 결과 달라질 수 있음

---

**예제 2️⃣ – JOIN + 서브쿼리 복합형**
```sql
SELECT E.ENAME, D.DNAME, E.SAL
FROM EMP E
LEFT JOIN (SELECT DEPTNO, AVG(SAL) AS AVG_SAL FROM EMP GROUP BY DEPTNO) A
ON E.DEPTNO = A.DEPTNO
WHERE E.SAL > A.AVG_SAL;
```
- 부서별 평균 급여보다 높은 사원 조회

---

**예제 3️⃣ – RANK / DENSE_RANK 비교**
```sql
SELECT DEPTNO, ENAME, SAL,
       RANK() OVER (PARTITION BY DEPTNO ORDER BY SAL DESC) AS R1,
       DENSE_RANK() OVER (PARTITION BY DEPTNO ORDER BY SAL DESC) AS R2
FROM EMP;
```
- 동일 급여 시 RANK는 순번 건너뜀, DENSE_RANK는 연속

---

**예제 4️⃣ – 트랜잭션 제어**
```sql
DELETE FROM EMP WHERE DEPTNO = 30;
ROLLBACK;
```
- COMMIT 전 상태로 복구됨 → DML 명령어 실행 후 확인 필수

---

## **🧾 모의시험 결과 요약**

| 구분 | 문항 | 주요 주제 | 결과 | 비고 |
|------|------|------------|-------|------|
| Part 1 | 01–05 | EXISTS / IN / 서브쿼리 | ✅ | 결과 차이 명확히 구분 |
| Part 2 | 06–10 | JOIN + 윈도우 함수 | ⚠ | OUTER JOIN 조건 위치 주의 |
| Part 3 | 11–15 | 트랜잭션 제어 | ✅ | COMMIT / ROLLBACK 구분 |
| Part 4 | 16–20 | 무결성 제약조건 / 집합 연산 | ✅ | 제약조건 검사 순서 숙지 |

📘 출처: 아이리포 SQLD 강의 – 복원 모의 2회 (유튜브 76~78강)

---

## **💬 느낀점 / 메모**

- 실전 응용형 문제는 **정답률보다 사고력 평가 비중이 높다.**  
- 단순 SELECT 중심에서 벗어나, **조건절·JOIN 순서·NULL 처리**를 정확히 해야 함.  
- 실수 방지를 위해 SQL 실행 순서를 머릿속에서 시각화하는 연습이 중요하다.  
- 내일은 전체 개념 복습 + 오답노트를 마지막으로 정리할 예정.

---

## **🔗 참고자료**
- 아이리포 SQLD 강의 – 복원 모의 2회 (유튜브 76~78강)
- SQLD 공식 교재 2권 p.371~380
- SQLD 실전 모의문제 PDF (Hyuncloud)

---

🧠 **Growth-Log용 학습주제**  
> Day 30 – 최종 모의시험 ② / 실전 응용형 문제 풀이  

💾 **오늘의 커밋 명령어**
```bash
git add .
git commit -m "chore(log): add SQLD Day 30 – 최종 모의시험 ② / 실전 응용형 문제 풀이 (2025-11-11)"
git push origin main
```
